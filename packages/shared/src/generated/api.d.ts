/**
 * Tipos generados automáticamente desde la spec OpenAPI del API.
 *
 * NO editar manualmente. Para regenerar:
 *
 *   pnpm generate:api-types
 *
 * El archivo se llena tras correr ese comando. En la primera generación,
 * exporta los `paths`/`components` típicos de openapi-typescript.
 *
 * Si este placeholder sigue acá, el comando aún no fue ejecutado.
 */
export type GeneratedPlaceholder = never;
